import React from 'react'

const ActionNotification = ({text}) => {
    return (
        <div>
            <h1>Action notification</h1>
            {text}
        </div>
    );
};

export default ActionNotification
